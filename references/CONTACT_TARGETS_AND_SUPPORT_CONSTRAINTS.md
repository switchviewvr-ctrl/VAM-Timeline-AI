# Contact Targets And Support Constraints

Contact/support is a relation to a target, not simply controller movement.

For example, `hands_on_partner_chest` requires:

- a partner/receiver context
- partner chest target evidence or a synthetic partner reference in generation
- left and right hand constraints to that target

Without the target, the action is not generation-ready for that support mode.
